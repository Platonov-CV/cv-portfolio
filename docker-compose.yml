services:
  portfolio:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped

  # box-measurement:
  #   build: ./projects/box-measurement
  #   expose:
  #     - "8001"
  #
  # bill-parser:
  #   build: ./projects/bill-parser
  #   expose:
  #     - "8002"
  #
  # fpv-tricks:
  #   build: ./projects/fpv-tricks
  #   expose:
  #     - "8003"
